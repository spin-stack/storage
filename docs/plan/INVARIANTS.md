# INVARIANTS

Invariants extracted from the architecture doc (§4/§5/§12/§14/§21/§27). Each is a
property a **checker** verifies inside the DST harness. An invariant is born
`pending` and becomes `active` when its checker runs in CI. The **standard gate**
(PLAN §2) requires all `active` checkers green.

**Checker states:** `pending` (no checker yet) · `active` (checker runs in CI) ·
`n/a-until` (subject does not exist yet; activated by the named phase).

| ID | State | Statement | Checker (how the harness verifies it) | Activated by | § |
|---|---|---|---|---|---|
| **INV-01** | **active** | **Simulable interfaces only.** No `time.Now()`, direct sockets, or disk/net/S3 syscalls outside the `simio` interfaces. | Static: custom `simulable` analyzer (`hack/analyzers/simulable`, golden-tested) + `forbidigo`/`depguard` in golangci-lint; `task lint` fails on any escape. Not a runtime checker. **Active since Increment 1.1.** | Phase 01 (1.1) ✓ | §25.1 |
| **INV-02** | **active** | **Deterministic replay.** Same seed + same inputs ⇒ identical harness trace and identical outcome. | `TestDeterministicReplay` runs each mandatory scenario twice per seed and byte-compares the event trace and pass/fail outcome. **Active since Increment 1.3.** | Phase 01 (1.3) ✓ | §25.1 |
| **INV-03** | **active** | **Ordered watermarks.** `published_sequence ≤ durable_sequence ≤ local_sequence` at every observation point. | `WatermarkOrderChecker` observes watermark events (emitted by the write-path scenario); `Log.AdvanceDurable/AdvancePublished` also enforce ordering at the source (`ErrWatermarkOrder`). **Active since 4.3.** | Phase 04 (4.3) ✓ | §5.6 |
| **INV-04** | **active** | **Unflushed bounds / backpressure.** `unflushed_bytes ≤ max_unflushed_bytes` and `oldest_unflushed_age ≤ max_unflushed_age`; exceeding ⇒ WRITEs rejected with explicit error, never silent NVMe fill. `Flush` clears the accounting once records are durable in S3. | `scenarioWALBackpressure` (byte/age limits ⇒ `ErrBackpressure`); `Flush` couples clearing to verified remote durability (§14.4). **Active since 4.3; remote-coupled since 6.2.** | Phase 04 (4.3) ✓; remote 6.2 ✓ | §5.7 |
| **INV-05** | **active** | **WAL serialize/replay is total and safe.** Any record sequence (write/discard/zeroes, arbitrary extents) replays to the expected state; arbitrary truncation at any byte and random bit corruption are detected by CRC and **never applied silently**. | `TestWALReplayProperty` (rapid): generate records → serialize → truncate at every byte + single-bit flip → replay; assert exact expected state (full/prefix) or a detected-corruption error, never wrong-and-silent. In the `task dst` set. **Active since Increment 4.2.** | Phase 04 (4.2) ✓ | §25.2, §14.1 |
| **INV-06** | **active (remote)** | **Durable ACK rule (mode-parametrized).** No FLUSH/FUA is ACKed unless, per the Agent's **monotonic clock**, its lease is valid at the instant of ACK (`remote` mode). In `local` mode the FLUSH ACK requires local `fdatasync` only; the lease rule still governs checkpoint/manifest/snapshot publication. | `DurableAckLeaseChecker` + `scenarioLeaseFencesDurableAck` + `TestFlushSelfFencesWhenLeaseExpired`: with an expired lease the object lands in S3 but the FLUSH returns `ErrSelfFenced`, `durable_sequence` does not advance, and the log self-fences. `local` mode ACKs regardless of the lease. **Active since 7.2.** | Phase 07 (7.2) ✓ | §12.2, §14.8 |
| **INV-07** | **active** | **FLUSH/FUA ordering.** ACK only after: capture target_seq → close batch → `fdatasync` ≤ target → PUTs ≤ target verified → lease valid → advance `durable_sequence`. No reordering that ACKs before durability. | `scenarioRemoteFlushOrdering` (verified-upload gates durable) + `scenarioLeaseFencesDurableAck`/`TestFlushSelfFences*` (the lease step). All six §14.4 steps now enforced. **Active since 6.2; lease step 5 since 7.2.** | Phase 06 (6.2) ✓ + Phase 07 (7.2) ✓ | §14.4 |
| **INV-08** | pending | **S3 is the recovery authority.** The durable point = end of the **longest contiguous prefix** of sequences under `wal/<vol>/<epoch>/` for the **highest fenced epoch**. PG watermarks are informative only. | Recovery checker recomputes the durable point purely from the simulated object store and asserts it ≥ every ACKed-durable write and independent of PG watermark values. | Phase 08 | §5.8, §22.1 |
| **INV-09** | **active** | **No ACKed-durable write lost across failover.** Every write ACKed durable by writer W1(epoch N) lies within the prefix the promoted writer W2(epoch N+1) recovers. | `NoLostAckedWriteChecker` + `scenarioFencedWriterNoLostAck`: W1 ACKs 1..N; partitioned (S3 intact) it self-fences on the next FLUSH (object lands in S3 but not ACKed); CP promotes W2 after FENCING_WAIT; `recovery.DurablePrefix` recovers ≥ W1's ACKed durable. Composes INV-06/07/10/11 + the contiguous-prefix recovery. **Active since 7.4.** | Phase 07 (7.4) ✓ | §5.1, §12.3–12.5 |
| **INV-10** | **active** | **Effective single writer.** At most one writer per volume publishes checkpoints/manifests/metadata for a given epoch; a stale-epoch Agent affects 0 rows / fails CAS. | `SingleWriterChecker` + `scenarioPromotionFencingWait` + `TestStaleWriterCannotPublishAfterPromotion`: after promotion a stale writer's `epoch.Verify` → `ErrEpochChanged`, its `CompareAndAdvance` → `ErrCASConflict`, and a stale-term PG mutation → `ErrStaleTerm`. **Active since 7.3** (CP-term half since 7.1). | Phase 07 (7.1+7.3) ✓ | §5.1, §7, §12.4 |
| **INV-11** | **active** | **Promotion wait.** The CP does not grant epoch N+1 until `last_renewal(W1) + lease_ttl + max_clock_skew` has elapsed. | `PromotionWaitChecker` + `scenarioPromotionFencingWait` + `TestFencingWaitEnforced`/`TestDriftOnlyLengthensTheWait`: `Promoter.Promote` returns `ErrFencingWaitNotElapsed` until the deadline; a lagging CP clock only waits longer, never grants early. **Active since 7.3.** | Phase 07 (7.3) ✓ | §12.3, §5.1 |
| **INV-12** | pending | **Recovery-point is the epoch boundary.** W2 writes `wal/<vol>/<N+1>/recovery-point.json {prev_epoch, recovered_up_to}`; late PUTs from W1 fall outside it. | Checker asserts recovery-point exists after promotion and that any post-fence W1 PUTs are outside the recovered prefix (and thus GC-eligible). | Phase 08 | §12.5 |
| **INV-13** | pending | **Never truncate WAL above unverified `published_sequence`.** Local WAL with `sequences ≤ published_sequence` is truncatable only after the corresponding checkpoint is **verified in S3**. | Checker intercepts every WAL truncation and asserts target ≤ verified published point; fault injection truncates aggressively and asserts recovery still succeeds. | Phase 10 (objectization/checkpoints) | §21.1, §14.7 |
| **INV-14** | pending | **GC cannot permanently delete.** GC only marks (tags / versioned delete markers); permanent removal is the bucket lifecycle. GC credentials lack `DeleteObject` permanent + Object-Lock bypass. | Checker: in the simulated object store, assert no GC action performs a permanent delete and that a live (reachable) object is never marked; assert the GC identity's capability set excludes permanent delete. | Phase 10 | §5.11, §21.3 |
| **INV-15** | **active** | **Nothing leaves the host in clear.** Every VM-data payload (WAL records, segments, checkpoints) is AES-256-GCM encrypted with the volume DEK before any PUT. Structural metadata (headers/manifests/descriptors) carries no guest data. | `NoPlaintextLeavesHostChecker` + `scenarioEncryptedWALNoPlaintextLeak`: writes a cleartext canary through an encrypted `Log`, asserts the WAL-object bytes (bound for S3) contain no canary, then replay+decrypt recovers it. Tamper fails closed. **Active since 5.2.** | Phase 05 (5.2) ✓ | §5.10, §15 |
| **INV-16** | pending | **Immutable snapshots.** After PUBLISHED, a snapshot's root, WAL objects, and segments never change. Compaction may replace objects with byte-for-byte logically-equal ones, never alter referenced logical content. | Checker snapshots the manifest closure at PUBLISHED and asserts referenced logical content is stable across later compaction/GC. | Phase 09/10 | §5.2 |
| **INV-17** | pending | **Background always yields.** Every Agent I/O belongs to a class (foreground/flush/background); background operates under a fixed NVMe+net budget and never degrades data-path latency. | Checker asserts each I/O op is tagged with a class and that background never exceeds its token bucket while foreground/flush are pending. | Phase 10 (first heavy background) | §5.9, §11 |
| **INV-18** | **active** | **S3 not in every WRITE.** A normal WRITE completes after local persistence; durability comes only from FLUSH/FUA/snapshot. | `scenarioWALWritePathNoPut` writes records then asserts the object store received 0 PUTs; the `Log` type structurally has no object store on the write path. **Active since 4.3.** | Phase 04 (4.3) ✓ | §5.3 |
| **INV-19** | pending | **Format read-old / write-new.** A vN+1 Agent reads all same-major formats; new formats are write-enabled only after the whole fleet can read them; the CP rejects incompatible placements by `max_format_version`. | Mixed-fleet test: writer at format v+1 blocked until all `hosts.max_format_version ≥ v+1`; a vN reader reads vN+1's read-compatible objects; incompatible placement/recovery rejected. | Phase 13 (fleet-mixed test); fields reserved Phase 04 | §27 |
| **INV-20** | pending | **PG is reconstructible from S3.** The full PostgreSQL state can be rebuilt from the self-describing S3 layout (`rebuild-metadata`). | Checker: wipe simulated PG, run `rebuild-metadata` from the object store, assert reconstructed catalog matches ground truth for the covered fields. | Phase 08 (basic) | §5.8 corollary, §22.5 |
| **INV-21** | **active (PUT)** | **Idempotent duplicated requests.** Duplicate data PUTs (deterministic key + `If-None-Match:*` + HEAD/checksum) and duplicate admin ops (`request_id`) produce no double effect; same range + different hash ⇒ hard fail. | `scenarioIdempotentBatchUpload` + `TestUpload*`: lost-response then retry yields exactly one object; a divergent object at the key hard-fails (`ErrDivergentObject`). Admin-op idempotency lands Phase 07. **Active since 6.2.** | Phase 06 (6.2) ✓ / admin Phase 07 | §14.5, §18 |

| **INV-22** | **active** | **All UUIDs are v7.** Every identifier is a UUIDv7 (time-ordered), in code and in Postgres. | Two layers (ADR-0007): (1) code — golangci `forbidigo` forbids `uuid.New`/`NewString`/`NewRandom` outside `internal/ids`, whose `ids.New()` returns v7; (2) DB — every uuid identity column has a `CHECK ((get_byte(uuid_send(col),6)>>4)=7)` that rejects non-v7 (proven by `TestPGRejectsNonV7`). **Active since 7.1.** | Phase 07 (7.1) ✓ | ADR-0007 |

## Notes on activation

- **Phase 01 activates the *framework*, not the data invariants.** It makes INV-01
  (lint) and INV-02 (deterministic replay) active, and ships the checker harness with
  at least one trivial checker wired end-to-end so later checkers are a fill-in, not a
  retrofit. This is the whole point of §25.1: "retrofitted checkers, no; the harness
  and checkers existing first, yes."
- Each later phase's `PHASE-0N.md` lists exactly which invariant IDs it flips from
  `pending`/`n/a-until` to `active`, and the standard gate then requires them.
- When an invariant's checker cannot yet be expressed (Harness-agent veto), the
  increment returns to the Planner rather than shipping the invariant as prose only.
